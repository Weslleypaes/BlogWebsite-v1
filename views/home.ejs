<%- include('partials/header.ejs') %>

    <h1>Inicio</h1>
    <p>
        <%= startingContent %>
    </p>

    <!-- <% for(var i=0; i < postsContent.length; i++){ %>
        <h2>
            <%= postsContent[i].title %>
        </h2>
        <p>
            <%= postsContent[i].content %>
        </p>
    <%  }  %> -->



    <% postsContent.forEach(function(post){ %>
        <% var length = 100; %>
        <% var content = post.content; %>
        <% var contentTruncated = content.substring(0, length); %>
        <h1>
            <%= post.title %>
        </h1>
        <p>
            <!-- <%= post.content %> -->
            <%= contentTruncated %> ... 
            <a href="/posts/<%= post.title %>" >Read More</a>
        </p>
        <% }); %>

            <%- include('partials/footer.ejs') %>